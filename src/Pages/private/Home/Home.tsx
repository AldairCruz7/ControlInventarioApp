import { Container, Typography } from '@mui/material';

const Home = () => {
    return (
        <Container>
            <Typography variant="h4" gutterBottom>
                Dashboard Principal
            </Typography>
        </Container>
    );
};

export default Home;
